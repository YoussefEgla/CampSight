<%- include("./partials/head") %> <%- include('./partials/navbar') %>

<h1>All Camp Sights</h1>
<section>
  <% for(let camp of camps) { %>
  <div>
    <img src="<%= camp.url %>" alt="<%= camp.name %>" />
    <h3><%= camp.name %></h3>
  </div>
  <%}%>
</section>

<%- include("./partials/footer") %>
